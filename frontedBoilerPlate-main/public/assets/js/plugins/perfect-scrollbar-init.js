$(function () {
    // const ps1 = new PerfectScrollbar('.trade-balance');    
    
    // Check if budget-content element exists before initializing
    if (document.querySelector('.budget-content')) {
    const ps2 = new PerfectScrollbar('.budget-content');
    }
    
    // Check if invoice-content element exists before initializing
    if (document.querySelector('.invoice-content')) {
    const ps3 = new PerfectScrollbar('.invoice-content');
    }
    
    // const ps3 = new PerfectScrollbar('.transaction-table');
});